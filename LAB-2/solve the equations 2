Given a and b, calculate the value of:

image  (a+b)/(a-b)

Display the value upto 2 decimal places.

Input Format

A single line containing two numbers a and b (in double).

Constraints

a!=b

Output Format

A single number (in double) denoting answer to the equation upto 2 decimal places.

Sample Input 0

2 3
Sample Output 0

-5.00


import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
       
        Scanner scn =new Scanner(System.in);
        double a = scn.nextDouble();
        double b = scn.nextDouble();
        double c = (a+b)/(a-b);
        System.out.format("%.2f",c);
    }
}