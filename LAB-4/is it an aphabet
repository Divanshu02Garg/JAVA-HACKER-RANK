Coderman is confused. The signals from the base have mixed characters but only alphabets are valid input. Given an input character, help Coderman determine whether it is a valid input or not.

Input Format

A single character

Constraints

None

Output Format

If the character is an alphabet print "Valid" else print "Invalid".

Sample Input 0

a
Sample Output 0

Valid

import java.util.*;
import java.io.*;
public class main{
    public static void main(String[] args){
        Scanner scn = new Scanner(System.in);
        char ch = scn.next().charAt(0);
        if(ch>='a' && ch<='z' || ch>='A' && ch<='Z'){
            System.out.println("Valid");
        }        
            else
                System.out.println("Invalid");
    }
}