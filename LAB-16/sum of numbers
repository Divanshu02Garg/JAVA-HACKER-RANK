// Overload the function sumOfNumbers for two cases:

// Case 1: Two integers are given as input
// Case 2: Three integers are given as input
// Return the sum of integers.

// Input Format

// Input is already handled in the code stub.

// Constraints

// None

// Output Format

// Output is handled in the code stub.


import java.util.*;

public class Solution {
	/* Enter your code here */
    
    public static void main(String[] args) {
    	Scanner sc = new Scanner(System.in);
        int a,b,c;
        a = sc.nextInt();
        b = sc.nextInt();
        c = sc.nextInt();
        System.out.println(sumOfNumbers(a,b));
        System.out.println(sumOfNumbers(a,b,c));
    }
    static int sumOfNumbers(int a,int b,int c)
    {
        int s=a+b+c;
        return s;
    }
    static int sumOfNumbers(int a,int b)
    {
        int s=a+b;
        return s;
    }
}